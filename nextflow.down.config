process {
  // Run all jobs in the Conda environment you created
  conda = "downstream.yml"

  // Run locally on your machine
  executor = 'local'

  // Default resources for each process (you can override per-process if needed)
  cpus   = 4
  memory = '8 GB'
  time   = '2h'
}

